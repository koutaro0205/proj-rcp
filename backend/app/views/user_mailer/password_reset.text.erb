パスワードをリセットするには、以下のリンクをクリックしてください。

<%= "http://localhost:8080/password-resets/edit?token=#{@user.reset_token}&email=#{@user.email}" %>

このリンクは2時間後に失効します。

パスワードのリセットを要求していない場合は、このメールを無視してください。
あなたのパスワードはそのまま残ります。
