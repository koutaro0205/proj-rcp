import { css } from '@emotion/css';

import space from '@/theme/space';

const styles = {
  checkbox: css({
    display: 'flex',
    alignItems: 'center',
    marginBottom: space.s,
  }),
};

export default styles;
