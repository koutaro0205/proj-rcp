import { css } from '@emotion/css';

const styles = {
  container: css({
    width: '50%',
  }),
  followStatusSection: css({
    height: '50px',
    display: 'flex',
    alignItems: 'center',
  }),
};

export default styles;
