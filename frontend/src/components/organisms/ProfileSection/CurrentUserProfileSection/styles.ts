import { css } from '@emotion/css';

import { SIDE_COLUMNS_CURRENT_USER_WIDTH } from '@/common/constants/flatDimention';

const styles = {
  container: css({
    width: SIDE_COLUMNS_CURRENT_USER_WIDTH,
  }),
};

export default styles;
