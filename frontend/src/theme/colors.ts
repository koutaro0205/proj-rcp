export type Color = keyof typeof colors;

const colors = {
  PrimaryColor: "#DED36E",
  baseColor: "#C6020C",
  transparent: "transparent",
  black: "#000000",
  white: "#FFFFFF",
  favorite: "gold",
};

export default colors;
