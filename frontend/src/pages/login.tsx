import { NextPage } from 'next';
import React from 'react';

import Layout from '@/components/templates/Layout';

const Login: NextPage = () => {
  return <Layout>ログインページ</Layout>;
};

export default Login;
